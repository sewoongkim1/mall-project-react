# 작업 로그 — 2026-04-01

## Phase 1: 환경설정 및 프로젝트 초기 세팅

### 1. 설계 문서 검토 및 수정

- `Docs/` 내 5개 문서(기능정의서, 사용자스토리, 화면설계, WBS, DB스키마) 전체 검토
- **database-schema.md 수정**:
  - 벡터 임베딩: "OpenAI text-embedding-3-small" → "Anthropic Claude API 기반 텍스트 분석 + 자체 임베딩 처리"로 변경
  - AI 추천 캐싱 전략 섹션 신규 추가 (MongoDB TTL 인덱스 기반, 향후 Redis 확장 고려)

### 2. 누락 설정 파일 생성

| 파일 | 설명 |
|------|------|
| `Client/postcss.config.js` | Tailwind CSS 빌드에 필수인 PostCSS 설정 |
| `Client/src/vite-env.d.ts` | Vite 타입 참조 (`import.meta.env` 지원) |
| `Server/.env` | 서버 환경변수 (개발용, MongoDB 로컬 기본값) |
| `Client/.env` | 클라이언트 환경변수 (Vite proxy 기본값) |

### 3. 불필요 파일 제거

- `prisma/` 디렉토리 삭제 (MongoDB + Mongoose 스택에서 불필요)

### 4. .gitignore 보완

- `.env.*.local`, `*.tsbuildinfo` 패턴 추가

### 5. 의존성 설치

- **Server**: `npm install` 완료
  - `multer-storage-cloudinary` 제거 (cloudinary v2와 peer dependency 충돌)
- **Client**: `npm install` 완료
- **Root**: `npm install` 완료 (concurrently)

### 6. TypeScript 타입 오류 수정 (5건)

| 파일 | 오류 | 수정 내용 |
|------|------|----------|
| `Server/src/controllers/auth.controller.ts` | jwt.sign expiresIn 타입 불일치 | `as jwt.SignOptions` 캐스팅 추가 |
| `Server/src/controllers/product.controller.ts` | `req.sessionID` 미존재 | `req.headers['x-session-id']`로 변경 |
| `Client/src/components/ui/index.ts` | JSX 포함 파일이 .ts 확장자 | `index.tsx`로 확장자 변경 |
| `Client/src/pages/ProductDetailPage.tsx` | 미사용 import, undefined 가능성 | 미사용 import 제거, null 체크 추가 |
| `Client/src/pages/HomePage.tsx`, `GNB.tsx`, `useProducts.ts` | 미사용 import | 불필요한 import 제거 |

### 7. 빌드 검증

- **Server**: `tsc --noEmit` 통과
- **Client**: `tsc --noEmit` + `vite build` 통과 (4.61s, 총 1680 modules)

### 8. 실행 확인

```bash
npm run dev  # concurrently로 Server(5000) + Client(5173) 동시 실행
```

---

## 현재 프로젝트 구조

```
mall-project-react/
├── Server/          # Node.js + Express + MongoDB (Mongoose)
│   ├── src/
│   │   ├── config/       # DB, Anthropic 설정
│   │   ├── controllers/  # auth, product, ai
│   │   ├── middleware/   # auth, errorHandler
│   │   ├── models/       # User, Product, Order, Seller, Review, BehaviorLog, Wishlist
│   │   └── routes/       # API 라우트
│   ├── .env
│   └── package.json
├── Client/          # React 18 + Vite + Tailwind CSS
│   ├── src/
│   │   ├── api/          # axios 인스턴스, API 함수
│   │   ├── components/   # ui + features (GNB, ProductCard, AI)
│   │   ├── hooks/        # React Query 훅
│   │   ├── pages/        # 6개 페이지
│   │   ├── store/        # Zustand (auth, cart)
│   │   ├── types/        # TypeScript 타입
│   │   └── utils/        # cn, formatPrice 등
│   ├── .env
│   └── package.json
├── Docs/            # 설계 문서
└── package.json     # 모노레포 루트 (workspaces)
```

---

## Phase 1 추가: 배포 환경 구성

### 1. 배포 플랫폼 설정

| 플랫폼 | 대상 | URL |
|--------|------|-----|
| Vercel | Frontend (Client) | https://mall-project-react.vercel.app |
| Render | Backend (Server) | https://mall-project-react.onrender.com |
| MongoDB Atlas | Database | mycluster.hggctv7.mongodb.net/styleai |

### 2. 배포 설정 파일 생성

| 파일 | 설명 |
|------|------|
| `Client/vercel.json` | Vercel 배포 설정 (SPA rewrite 포함) |
| `render.yaml` | Render 블루프린트 (서버 배포 설정) |
| `Client/.env.production.example` | 프론트 프로덕션 환경변수 가이드 |
| `Server/.env.production.example` | 서버 프로덕션 환경변수 가이드 |
| `scripts/test-deploy.sh` | 배포 검증 테스트 스크립트 |

### 3. 서버 수정사항

- CORS: 다중 origin 지원 (쉼표 구분)
- Health check: DB 상태 포함 (`/health`)
- 서버 포트: 5000 → 5001 (시스템 서비스 충돌 회피)
- 쿠키 `sameSite`: 프로덕션 cross-origin 대응 (`none`/`lax`)
- AI 추천 API: API 키 미설정 시 빈 배열 반환 (500 에러 방지)

### 4. 배포 연결 테스트 결과

- Backend Health Check: **200 OK** + DB connected
- Frontend 페이지 로드: **정상**
- CORS 헤더: Vercel origin 허용 확인
- Vite Proxy 연동: **정상**

---

## Phase 2: 인증/회원 시스템

### 1. 인증 API 동작 검증

기존 코드 검증 후 수정:

| API | 상태 |
|-----|------|
| POST `/api/auth/register` | **정상** (회원가입 + JWT 발급) |
| POST `/api/auth/login` | **정상** (로그인 + JWT 발급) |
| POST `/api/auth/logout` | **정상** (쿠키 제거) |
| GET `/api/auth/me` | **정상** (토큰 인증) |
| PUT `/api/auth/preferences` | **정상** (AI 취향 저장) |

### 2. 프론트 수정사항

- `RegisterPage`: 회원가입 성공 후 step 전환 로직 수정 (비동기 대응)
- `useRegister`: 불필요한 navigate 제거
- `useLogin`: 환영 메시지 토스트 (`{닉네임}님 환영합니다!`)
- `App.tsx`: `/mypage` 라우트 추가
- `MyPage.tsx`: 프로필 카드, 메뉴, 로그아웃 신규 생성

### 3. 소셜 로그인 (카카오 + 구글)

| 항목 | 카카오 | 구글 |
|------|--------|------|
| OAuth Flow | Authorization Code | Authorization Code |
| 인증 URL | `/api/auth/kakao` | `/api/auth/google` |
| 콜백 URL | `/api/auth/kakao/callback` | `/api/auth/google/callback` |
| 프론트 콜백 | `/auth/social-callback` | `/auth/social-callback` |
| 상태 | **동작 확인** | **동작 확인** |

생성 파일:
- `Server/src/controllers/social-auth.controller.ts` — OAuth 컨트롤러
- `Client/src/pages/SocialCallbackPage.tsx` — 소셜 콜백 처리

### 4. 권한 관리 시스템 (셀러/관리자)

**API 엔드포인트:**

| API | 권한 | 설명 |
|-----|------|------|
| POST `/api/seller/apply` | 로그인 | 셀러 신청 |
| GET `/api/seller/my-status` | 로그인 | 내 셀러 상태 조회 |
| PUT `/api/seller/my-info` | 셀러 | 셀러 정보 수정 |
| GET `/api/admin/sellers` | 관리자 | 셀러 목록 조회 |
| PUT `/api/admin/sellers/:id` | 관리자 | 셀러 승인/거절 |
| GET `/api/admin/users` | 관리자 | 회원 목록 조회 |
| PUT `/api/admin/users/:id/role` | 관리자 | 회원 역할 변경 |

**프론트 페이지:**

| 경로 | 페이지 | 접근 권한 |
|------|--------|-----------|
| `/seller/apply` | 셀러 신청 | 로그인 유저 |
| `/seller` | 셀러 대시보드 | SELLER/ADMIN |
| `/seller/settings` | 스토어 설정 (정보 수정) | SELLER/ADMIN |
| `/admin` | 관리자 대시보드 (셀러/회원 관리) | ADMIN |
| `/mypage` | 마이페이지 | 로그인 유저 |

**GNB 변경:**
- SELLER 역할 → "셀러 관리" 링크 표시
- ADMIN 역할 → "Admin" 링크 표시

---

## 커밋 히스토리

| 커밋 | 내용 |
|------|------|
| `0f006dd` | 프로젝트 초기 세팅 |
| `7dc1d3c` | 배포 환경 구성 (Vercel + Render) |
| `eed3d0d` | Render 빌드 devDependencies 수정 |
| `bcef871` | 인증 시스템 검증 + MyPage 추가 |
| `f80ffc8` | 소셜 로그인 (카카오 + 구글 OAuth) |
| `7c10acd` | 셀러/관리자 권한 관리 시스템 |

---

## 테스트 계정

| 이메일 | 비밀번호 | 역할 |
|--------|----------|------|
| test@styleai.com | Test1234! | ADMIN |
| test2@styleai.com | Test1234! | SELLER |

---

## 다음 단계

- **Phase 3**: 상품 등록, 검색, 장바구니, 결제
- 셀러 상품 등록/관리 페이지 (`/seller/products`)
- 상품 검색/필터링
- 장바구니 + 토스페이먼츠 결제 연동

# 작업 로그 — 2026-04-01

## Phase 1: 환경설정 및 프로젝트 초기 세팅

### 1. 설계 문서 검토 및 수정

- `Docs/` 내 5개 문서(기능정의서, 사용자스토리, 화면설계, WBS, DB스키마) 전체 검토
- **database-schema.md 수정**:
  - 벡터 임베딩: "OpenAI text-embedding-3-small" → "Anthropic Claude API 기반 텍스트 분석 + 자체 임베딩 처리"로 변경
  - AI 추천 캐싱 전략 섹션 신규 추가 (MongoDB TTL 인덱스 기반, 향후 Redis 확장 고려)

### 2. 누락 설정 파일 생성

| 파일 | 설명 |
|------|------|
| `Client/postcss.config.js` | Tailwind CSS 빌드에 필수인 PostCSS 설정 |
| `Client/src/vite-env.d.ts` | Vite 타입 참조 (`import.meta.env` 지원) |
| `Server/.env` | 서버 환경변수 (개발용, MongoDB 로컬 기본값) |
| `Client/.env` | 클라이언트 환경변수 (Vite proxy 기본값) |

### 3. 불필요 파일 제거

- `prisma/` 디렉토리 삭제 (MongoDB + Mongoose 스택에서 불필요)

### 4. .gitignore 보완

- `.env.*.local`, `*.tsbuildinfo` 패턴 추가

### 5. 의존성 설치

- **Server**: `npm install` 완료
  - `multer-storage-cloudinary` 제거 (cloudinary v2와 peer dependency 충돌)
- **Client**: `npm install` 완료
- **Root**: `npm install` 완료 (concurrently)

### 6. TypeScript 타입 오류 수정 (5건)

| 파일 | 오류 | 수정 내용 |
|------|------|----------|
| `Server/src/controllers/auth.controller.ts` | jwt.sign expiresIn 타입 불일치 | `as jwt.SignOptions` 캐스팅 추가 |
| `Server/src/controllers/product.controller.ts` | `req.sessionID` 미존재 | `req.headers['x-session-id']`로 변경 |
| `Client/src/components/ui/index.ts` | JSX 포함 파일이 .ts 확장자 | `index.tsx`로 확장자 변경 |
| `Client/src/pages/ProductDetailPage.tsx` | 미사용 import, undefined 가능성 | 미사용 import 제거, null 체크 추가 |
| `Client/src/pages/HomePage.tsx`, `GNB.tsx`, `useProducts.ts` | 미사용 import | 불필요한 import 제거 |

### 7. 빌드 검증

- **Server**: `tsc --noEmit` 통과
- **Client**: `tsc --noEmit` + `vite build` 통과 (4.61s, 총 1680 modules)

### 8. 실행 확인

```bash
npm run dev  # concurrently로 Server(5000) + Client(5173) 동시 실행
```

---

## 현재 프로젝트 구조

```
mall-project-react/
├── Server/          # Node.js + Express + MongoDB (Mongoose)
│   ├── src/
│   │   ├── config/       # DB, Anthropic 설정
│   │   ├── controllers/  # auth, product, ai
│   │   ├── middleware/   # auth, errorHandler
│   │   ├── models/       # User, Product, Order, Seller, Review, BehaviorLog, Wishlist
│   │   └── routes/       # API 라우트
│   ├── .env
│   └── package.json
├── Client/          # React 18 + Vite + Tailwind CSS
│   ├── src/
│   │   ├── api/          # axios 인스턴스, API 함수
│   │   ├── components/   # ui + features (GNB, ProductCard, AI)
│   │   ├── hooks/        # React Query 훅
│   │   ├── pages/        # 6개 페이지
│   │   ├── store/        # Zustand (auth, cart)
│   │   ├── types/        # TypeScript 타입
│   │   └── utils/        # cn, formatPrice 등
│   ├── .env
│   └── package.json
├── Docs/            # 설계 문서
└── package.json     # 모노레포 루트 (workspaces)
```
